Given /^that project have a card$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the project must have no cards$/ do
  pending # express the regexp above with the code you wish you had
end